# 运行产物

本目录保存本地验证和 CI 运行产生的临时结果，不属于源码、测试代码、编译目录或安装目录。

```text
artifacts/
├── benchmark-results/   # 性能基准 CSV/JSON
└── local-acceptance/    # 本地验收日志和证据
```

两个子目录均默认被 Git 忽略。需要提交发布证据时，应由发布流程显式选择并打包，不应把它们作为项目源码的一部分提交。
