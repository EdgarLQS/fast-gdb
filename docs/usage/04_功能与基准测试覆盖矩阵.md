# 功能与基准测试覆盖矩阵

## 1. 产品覆盖

| 能力 | 单元 | GDAL parity | 真实数据 | 性能 |
|---|---:|---:|---:|---:|
| Catalog/System tables | 是 | 是 | 是 | 否 |
| Table/Tablx/FID | 是 | 是 | 是 | 是 |
| Fields/NULL/Binary | 是 | 是 | 是 | 是 |
| Geometry/WKB | 是 | 是 | 是 | 是 |
| FeatureCursor | 是 | 是 | 是 | 是 |
| WHERE/.atx | 是 | 是 | 是 | 是 |
| Spatial/.spx | 是 | 是 | 是 | 是 |
| SpatialWhere | 是 | 是 | 是 | 是 |
| Hybrid fallback | 是 | 是 | 是 | 是 |
| MultiPatch | 部分 | 是 | 待扩展 | 否 |

## 2. 外部编辑兼容性

| GDAL 操作 | Reader 关闭 | GDALClose | Reader 重开 | 状态 |
|---|---:|---:|---:|---|
| SetFeature | 必须 | 必须 | 验证新值 | 已建基础门禁 |
| CreateFeature | 必须 | 必须 | 验证新 FID/记录 | 待扩展 |
| DeleteFeature | 必须 | 必须 | 验证删除槽 | 待扩展 |
| Create/Delete Field | 必须 | 必须 | 验证 Schema | 待扩展 |
| Create/Delete Index | 必须 | 必须 | 验证索引/回退 | 待扩展 |
| REPACK | 必须 | 必须 | 验证 tablx/FID | 待扩展 |
| Recompute extent | 必须 | 必须 | 验证范围 | 待扩展 |

## 3. 并发边界

| 场景 | 类型 | 断言 |
|---|---|---|
| Reader 关闭后 GDAL 写、再重开 | 正式门禁 | 必须读取新数据 |
| 已有 Reader + GDAL Writer | Characterization | 只记录 old/new/mixed/error |
| Writer 打开时新 Reader | Characterization | 只记录 old/new/mixed/error |
| GDALClose 后旧 Reader | Characterization | 只记录，不支持复用 |
| 销毁旧 Reader 后新开 | 正式门禁 | 必须读取新数据 |

## 4. 构建矩阵

| 构建 | GDAL | 完整测试 | 目的 |
|---|---:|---:|---|
| Linear release | OFF | OFF/ON | 纯 Reader 和安装面 |
| Hybrid release | ON | OFF/ON | GDAL fallback |
| Boundary | ON | OFF | 聚焦读写生命周期 |
| Full | ON | ON | Reader 回归与 parity |
| Sanitizer | 可选 | 选择性 | ASan/UBSan/LSan |

## 5. 性能门禁

性能结果只有在以下条件全部满足时有效：

- 记录数和 FID 一致；
- 字段校验和一致；
- WKB/几何一致；
- final match 与 GDAL 一致；
- 没有 Reader/GDAL update 生命周期重叠；
- 记录 commit、平台、编译器、GDAL、manifest 和缓存状态。

## 6. 明确删除的测试类别

- 自研 Writer 功能测试；
- VersionedGdbStore；
- Append/Update/Delete/Transaction/Recovery；
- Writer 性能；
- Writer package consumer；
- Writer macOS 专项工作流。

GDAL 写入测试的目标是验证 fast-gdb Reader 兼容性，不代表 fast-gdb 提供 Writer。
