# 09 — 本地验收说明：M18 Writer 收口与 Reader 10M fresh-open

- **适用分支**：`codex/m18-main-closeout`
- **对比基线**：`main@42d8f76620a8c39eeb8523a0f84fcde0eb719f01`
- **目标**：在本地完成 GitHub Actions 当前无法执行的构建、合同、package consumer、性能和 profile 验收
- **当前总体状态**：Writer 代码已实现但正式运行证据缺失；Reader 工具已完成但运行验收和 profile 驱动优化未完成

本文档区分“已经完成、无需重复开发的内容”和“必须由本地环境执行的验收任务”。本地验收不得通过放宽阈值、删除精确判断或忽略 required `SKIP` 获得表面通过。

## 1. 已经完成的内容

以下内容已在 `codex/m18-main-closeout` 上完成，本地工作重点是验证，不是重新实现。

### 1.1 M18 Writer

- Append、Update、Delete、Transaction 和 Recovery 已进入实现基线；
- 稳定 Writer API、legacy API 和安装边界已整理；
- Writer 当前状态、roadmap、limitations、overview 和验收记录已同步；
- PR #11 已关闭为历史交付入口，Issue #12 保持 Open；
- 当前结论固定为 `Code accepted / Formal acceptance blocked`，直到本地和正式 artifact 齐全。

### 1.2 Reader 验收工具

`scripts/run_spatial_regression.py` 已支持：

- 重复 `--dataset LABEL=PATH`；
- `steady-state` 和 `fresh-open`；
- current/main 交替执行；
- CSV、JSON、环境摘要和原始日志；
- `invalid_geometries`、result count、可用时 FID 签名和 5% current/main 回退门禁；
- 原 `--gdb` 参数兼容。

`tests/scripts/test_run_spatial_regression.py` 已覆盖参数解析、动态导入、回退阈值、result count 和 FID 签名判定。

### 1.3 已明确不做的内容

没有当前 profile 证据时，不实施 Reader 算法优化。Linux、Windows、50M、35GB/5 亿、原生曲线写入和 MultiPatch 写入不属于本轮验收。

## 2. 本地验收环境

优先使用 macOS Apple Silicon 或 Intel macOS。建议安装：

```bash
brew install ninja cmake gdal python@3.12
```

确认环境：

```bash
set -euo pipefail
sw_vers
uname -m
clang++ --version
cmake --version
ninja --version
python3 --version
gdalinfo --version
ogrinfo --formats | tee local-acceptance-gdal-formats.log
grep -q 'OpenFileGDB' local-acceptance-gdal-formats.log
```

任何一项失败都先解决依赖，不进入正式验收。

## 3. 获取和固定代码

```bash
git fetch origin
git switch codex/m18-main-closeout
git pull --ff-only origin codex/m18-main-closeout
git status --short
git rev-parse HEAD | tee local-acceptance-head.txt
git rev-parse origin/main | tee local-acceptance-main.txt
git rev-list --left-right --count origin/main...HEAD
```

要求：

- 工作区干净；
- 当前分支正确；
- `HEAD` 不落后于远端同名分支；
- 保存 current SHA 和 main SHA；
- 不在验收过程中修改源码。

创建统一证据目录：

```bash
export FAST_GDB_ACCEPTANCE_ROOT="$PWD/local-acceptance/$(date +%Y%m%d-%H%M%S)"
mkdir -p "$FAST_GDB_ACCEPTANCE_ROOT"
cp local-acceptance-head.txt local-acceptance-main.txt local-acceptance-gdal-formats.log \
  "$FAST_GDB_ACCEPTANCE_ROOT/"
```

`local-acceptance/` 仅为本地证据，不提交仓库。

## 4. 第一门禁：Python 静态测试

```bash
python3 -m unittest -v tests/scripts/test_run_spatial_regression.py \
  2>&1 | tee "$FAST_GDB_ACCEPTANCE_ROOT/python-tests.log"
```

完成判据：

- 进程退出码为 0；
- 所有测试均为 `ok`；
- 无 import、dataclass、参数解析或比较函数异常。

## 5. 第二门禁：GDAL ON Release 与完整 CTest

```bash
cmake -S . -B build-accept-gdal -G Ninja \
  -DCMAKE_BUILD_TYPE=Release \
  -DFAST_GDB_WITH_GDAL=ON \
  -DFAST_GDB_CURVE_BACKEND=BUILTIN \
  -DFAST_GDB_GEOMETRY_OUTPUT=STANDARD_WKB \
  -DFAST_GDB_BUILD_TOOLS=OFF \
  -DFAST_GDB_BUILD_FULL_TESTS=ON \
  -DBUILD_TESTING=ON \
  2>&1 | tee "$FAST_GDB_ACCEPTANCE_ROOT/configure-gdal.log"

cmake --build build-accept-gdal --parallel \
  2>&1 | tee "$FAST_GDB_ACCEPTANCE_ROOT/build-gdal.log"

ctest --test-dir build-accept-gdal --output-on-failure -C Release \
  2>&1 | tee "$FAST_GDB_ACCEPTANCE_ROOT/ctest-gdal.log"
```

完成判据：Release 配置、完整构建和完整 CTest 全部退出码 0；required 测试不得无原因 `SKIP`。

## 6. 第三门禁：GDAL OFF Release 与完整 CTest

```bash
cmake -S . -B build-accept-nogdal -G Ninja \
  -DCMAKE_BUILD_TYPE=Release \
  -DFAST_GDB_WITH_GDAL=OFF \
  -DFAST_GDB_CURVE_BACKEND=BUILTIN \
  -DFAST_GDB_GEOMETRY_OUTPUT=STANDARD_WKB \
  -DFAST_GDB_BUILD_TOOLS=OFF \
  -DFAST_GDB_BUILD_FULL_TESTS=ON \
  -DFAST_GDB_INSTALL_LEGACY_WRITER_API=ON \
  -DBUILD_TESTING=ON \
  2>&1 | tee "$FAST_GDB_ACCEPTANCE_ROOT/configure-nogdal.log"

cmake --build build-accept-nogdal --parallel \
  2>&1 | tee "$FAST_GDB_ACCEPTANCE_ROOT/build-nogdal.log"

ctest --test-dir build-accept-nogdal --output-on-failure -C Release \
  2>&1 | tee "$FAST_GDB_ACCEPTANCE_ROOT/ctest-nogdal.log"
```

完成判据同上。若某测试按设计只支持 GDAL，必须在最终报告中列出测试名、SKIP 原因和依据，不能计入 PASS。

## 7. 第四门禁：五套 Writer 合同各连续三次

使用 GDAL ON 的完整测试 runner：

```bash
export WRITER_RUNNER="$PWD/build-accept-gdal/bin/gdb_tutorial_test_runner"
test -x "$WRITER_RUNNER"
```

依次执行：

```bash
for manifest in \
  writer-macos-v2.json \
  writer-append-macos-v1.json \
  writer-update-macos-v1.json \
  writer-delete-macos-v1.json \
  writer-transaction-macos-v1.json
do
  name="${manifest%.json}"
  python3 scripts/run_test_contract.py \
    --manifest "tests/contracts/$manifest" \
    --full-test-binary "$WRITER_RUNNER" \
    --workspace "$PWD" \
    --output-dir "$FAST_GDB_ACCEPTANCE_ROOT/$name" \
    --build-type Release \
    --repeat 3 \
    2>&1 | tee "$FAST_GDB_ACCEPTANCE_ROOT/$name.log"
done
```

完成判据：

- 五套合同各连续三次成功；
- required 场景均 PASS；
- 输出目录包含 manifest snapshot、每次运行日志和汇总；
- Append、Update、Delete、Transaction/Recovery 结果不存在间歇失败；
- recovery、publish、rollback、cleanup 相关断言无失败。

## 8. 第五门禁：安装边界和 package consumer

### 8.1 no-GDAL stable + legacy

```bash
cmake --build build-accept-nogdal --target explorgdb_writer_lib --parallel
cmake --install build-accept-nogdal --prefix "$PWD/install-accept-nogdal"

stable="$PWD/install-accept-nogdal/include/fast_gdb/writer"
legacy="$PWD/install-accept-nogdal/include/fast_gdb/writer_legacy"
test -f "$stable/writer_session.h"
test ! -e "$stable/writer_index.h"
test ! -e "$stable/writer_append.h"
test -f "$legacy/gdb_table_writer.h"
test -f "$legacy/row_buffer.h"
test -f "$legacy/tablx_writer.h"

cmake -S tests/package_consumer -B build-consumer-nogdal -G Ninja \
  -DCMAKE_BUILD_TYPE=Release \
  -DCMAKE_PREFIX_PATH="$PWD/install-accept-nogdal" \
  -DFAST_GDB_CONSUMER_TARGET=writer
cmake --build build-consumer-nogdal --parallel
./build-consumer-nogdal/fast_gdb_package_consumer

cmake -S tests/package_consumer -B build-consumer-legacy -G Ninja \
  -DCMAKE_BUILD_TYPE=Release \
  -DCMAKE_PREFIX_PATH="$PWD/install-accept-nogdal" \
  -DFAST_GDB_CONSUMER_TARGET=writer_legacy
cmake --build build-consumer-legacy --parallel
./build-consumer-legacy/fast_gdb_package_consumer
```

### 8.2 GDAL stable Writer

```bash
cmake --build build-accept-gdal --target explorgdb_writer_lib --parallel
cmake --install build-accept-gdal --prefix "$PWD/install-accept-gdal"

stable="$PWD/install-accept-gdal/include/fast_gdb/writer"
test -f "$stable/writer_session.h"
test -f "$stable/writer_index.h"
test -f "$stable/writer_append.h"
test -f "$stable/writer_update.h"
test -f "$stable/writer_delete.h"
test -f "$stable/writer_transaction.h"
test -f "$stable/writer_recovery.h"
test ! -e "$stable/gdb_table_writer.h"

cmake -S tests/package_consumer -B build-consumer-gdal -G Ninja \
  -DCMAKE_BUILD_TYPE=Release \
  -DCMAKE_PREFIX_PATH="$PWD/install-accept-gdal" \
  -DFAST_GDB_CONSUMER_TARGET=writer \
  -DFAST_GDB_CONSUMER_CHECK_INDEX=ON \
  -DFAST_GDB_CONSUMER_CHECK_APPEND=ON \
  -DFAST_GDB_CONSUMER_CHECK_TRANSACTION=ON
cmake --build build-consumer-gdal --parallel
./build-consumer-gdal/fast_gdb_package_consumer
```

完成判据：三类 consumer 都能独立 configure、build、run；稳定目录不泄漏 legacy/private 头文件。

## 9. 第六门禁：Writer current/main/GDAL 性能

准备 main baseline：

```bash
git worktree add --detach baseline-main origin/main
```

构建 current 和 main：

```bash
cmake -S . -B build-perf-current -G Ninja \
  -DCMAKE_BUILD_TYPE=Release -DFAST_GDB_WITH_GDAL=ON \
  -DFAST_GDB_CURVE_BACKEND=BUILTIN \
  -DFAST_GDB_GEOMETRY_OUTPUT=STANDARD_WKB \
  -DFAST_GDB_BUILD_TOOLS=OFF -DFAST_GDB_BUILD_FULL_TESTS=ON \
  -DBUILD_TESTING=ON
cmake --build build-perf-current --target gdb_tutorial_test_runner --parallel

cmake -S baseline-main -B build-perf-main -G Ninja \
  -DCMAKE_BUILD_TYPE=Release -DFAST_GDB_WITH_GDAL=ON \
  -DFAST_GDB_CURVE_BACKEND=BUILTIN \
  -DFAST_GDB_GEOMETRY_OUTPUT=STANDARD_WKB \
  -DFAST_GDB_BUILD_TOOLS=OFF -DFAST_GDB_BUILD_FULL_TESTS=ON \
  -DBUILD_TESTING=ON
cmake --build build-perf-main --target gdb_tutorial_test_runner --parallel
```

运行固定 Writer 矩阵：

```bash
export FAST_GDB_PERF_FILTER='PerformanceBenchmarkFixture.W7_WideAttributes_*:PerformanceBenchmarkFixture.W8_Geometry_*:PerformanceBenchmarkFixture.W9_Dimension_*'
mkdir -p "$FAST_GDB_ACCEPTANCE_ROOT/writer-performance/current" \
         "$FAST_GDB_ACCEPTANCE_ROOT/writer-performance/main"

FAST_GDB_RUN_FULL_BENCHMARKS=1 \
FAST_GDB_BENCHMARK_SAMPLES=3 \
FAST_GDB_BENCHMARK_OUTPUT_DIR="$FAST_GDB_ACCEPTANCE_ROOT/writer-performance/current" \
FAST_GDB_BENCHMARK_CODE_VERSION="$(git rev-parse HEAD)" \
FAST_GDB_BENCHMARK_CACHE_STATE=warm-recreate \
build-perf-current/bin/gdb_tutorial_test_runner \
  --gtest_filter="$FAST_GDB_PERF_FILTER" --gtest_color=no \
  --gtest_output=json:"$FAST_GDB_ACCEPTANCE_ROOT/writer-performance/current/gtest.json" \
  | tee "$FAST_GDB_ACCEPTANCE_ROOT/writer-performance/current/benchmark.log"

FAST_GDB_RUN_FULL_BENCHMARKS=1 \
FAST_GDB_BENCHMARK_SAMPLES=3 \
FAST_GDB_BENCHMARK_OUTPUT_DIR="$FAST_GDB_ACCEPTANCE_ROOT/writer-performance/main" \
FAST_GDB_BENCHMARK_CODE_VERSION="$(git -C baseline-main rev-parse HEAD)" \
FAST_GDB_BENCHMARK_CACHE_STATE=warm-recreate \
build-perf-main/bin/gdb_tutorial_test_runner \
  --gtest_filter="$FAST_GDB_PERF_FILTER" --gtest_color=no \
  --gtest_output=json:"$FAST_GDB_ACCEPTANCE_ROOT/writer-performance/main/gtest.json" \
  | tee "$FAST_GDB_ACCEPTANCE_ROOT/writer-performance/main/benchmark.log"

python3 scripts/compare_writer_performance.py \
  --manifest tests/contracts/writer-macos-performance-v1.json \
  --current "$FAST_GDB_ACCEPTANCE_ROOT/writer-performance/current" \
  --main "$FAST_GDB_ACCEPTANCE_ROOT/writer-performance/main" \
  --output-dir "$FAST_GDB_ACCEPTANCE_ROOT/writer-performance/comparison"
```

完成判据：比较脚本退出码 0；current 不超过 manifest 允许的回退；所有 correctness 前置条件成立。

## 10. 第七门禁：Writer profile

```bash
cmake --install build-perf-current --prefix "$PWD/install-perf-current"
cmake -S tools/writer_profile -B build-writer-profile -G Ninja \
  -DCMAKE_BUILD_TYPE=Release \
  -DCMAKE_PREFIX_PATH="$PWD/install-perf-current"
cmake --build build-writer-profile --parallel

python3 scripts/run_macos_sample_profile.py \
  --scenario-id P0-WIDE-100-PROFILE \
  --output-dir "$FAST_GDB_ACCEPTANCE_ROOT/writer-performance/profile" \
  --duration-seconds 5 --interval-ms 1 -- \
  build-writer-profile/writer_profile_driver \
    --workload wide-100 --rows 100000 \
    --output "$FAST_GDB_ACCEPTANCE_ROOT/writer-performance/profile/P0-WIDE-100-PROFILE.phases.json"

python3 scripts/run_macos_sample_profile.py \
  --scenario-id P0-POINT-XYZM-PROFILE \
  --output-dir "$FAST_GDB_ACCEPTANCE_ROOT/writer-performance/profile" \
  --duration-seconds 5 --interval-ms 1 -- \
  build-writer-profile/writer_profile_driver \
    --workload point-xyzm --rows 2000000 \
    --output "$FAST_GDB_ACCEPTANCE_ROOT/writer-performance/profile/P0-POINT-XYZM-PROFILE.phases.json"
```

完成判据：两个 profile 均有原始 sample、phase JSON、环境信息和可解释热点；不能只有汇总数字。

## 11. 第八门禁：Reader 三类 10M fresh-open

本地必须存在以下 fixture，且不得提交：

```text
test_data/spatial_matrix/point_10000000.gdb
test_data/spatial_matrix/multipoint_10000000.gdb
test_data/spatial_matrix/line_10000000.gdb
```

运行：

```bash
python3 scripts/run_spatial_regression.py \
  --current-build build-perf-current \
  --baseline-build build-perf-main \
  --mode fresh-open \
  --trials 5 \
  --max-regression 0.05 \
  --dataset Point=test_data/spatial_matrix/point_10000000.gdb \
  --dataset MultiPoint=test_data/spatial_matrix/multipoint_10000000.gdb \
  --dataset Polyline=test_data/spatial_matrix/line_10000000.gdb \
  --output "$FAST_GDB_ACCEPTANCE_ROOT/reader-10m-fresh-open" \
  2>&1 | tee "$FAST_GDB_ACCEPTANCE_ROOT/reader-10m-fresh-open/run.log"
```

完成判据：

- 15 档均完成 5 次采样；
- `invalid_geometries=0`；
- current/main result count 一致；
- 输出提供 FID 签名时必须一致；
- current 相对 main 无超过 5% 回退；
- CSV、JSON、environment.json 和 30 份 current/main 原始日志齐全；
- fresh-open 必须注明“未清理 OS page cache，非 strict-cold”。

该工具当前主要执行 current/main 回退门禁。fast-gdb 与 GDAL 的绝对门禁仍以 benchmark 输出和计划中既有规则判定，不能仅凭脚本退出码声明全面优于 GDAL。

## 12. 第九门禁：Reader profile 和算法优化准入

优先 profile：

- Point：10%、30%、80%、100%；
- MultiPoint：10%、30%；
- Polyline：30%。

至少拆分：catalog、table/tablx open、`.spx` open/mmap、candidate lookup、FID 定位、blob 读取、decode、bbox/predicate、结果收集和排序去重。

完成判据：

- 每个目标场景有重复 profile；
- 只有稳定占总耗时约 20% 以上的阶段可进入优化；
- 若没有明确热点，则记录“无优化准入”，不得猜测修改算法；
- 若实施优化，目标场景稳定改善至少 5%，完整 FID 一致，`invalid_geometries=0`，其他档位无超过 5% 回退；
- 不满足条件的优化必须撤销并记录。

## 13. 本地验收结果判定

### 13.1 可判定为本地 Accepted

必须同时满足：

- Python 测试通过；
- GDAL ON/OFF Release 和完整 CTest 通过；
- 五套 Writer 合同各连续三次通过；
- no-GDAL、legacy、GDAL consumer 全部通过；
- Writer 性能比较和两个 profile 完成；
- Reader 15 档 fresh-open 完成且正确性、current/main 回退门禁通过；
- Reader profile 已完成，并对“优化或不优化”给出证据化结论；
- 无 required 无原因 `SKIP`；
- 证据绑定 current/main SHA、机器和依赖版本。

### 13.2 只能判定部分通过

任一性能、profile、fixture 或合同缺失时，必须使用以下分项结论：

- Writer：`Code accepted / Local formal acceptance blocked`；
- Reader 工具：`Tooling accepted / Runtime acceptance blocked`；
- Reader 优化：`Not admitted`、`In progress` 或 `Rejected`；
- 整体：`Not Accepted`。

## 14. 验收结果回传格式

验收后至少提供：

```text
当前 SHA：
main SHA：
系统/架构：
Clang/CMake/Ninja/Python/GDAL：
Python 测试：PASS/FAIL
GDAL ON CTest：PASS/FAIL/SKIP 列表
GDAL OFF CTest：PASS/FAIL/SKIP 列表
五套 Writer 合同：每套 3/3 或失败次数
package consumers：no-GDAL / legacy / GDAL
Writer 性能比较：PASS/FAIL
Writer profiles：完成/缺失
Reader 15 档：PASS/FAIL，失败档列表
Reader profiles：热点列表或无优化准入
本地最终判定：
证据目录：
```

不要只发送截图。优先打包 `local-acceptance/<timestamp>/`，并保留完整日志、JSON、CSV 和 profile 原始文件。

## 15. 清理

验收结果确认后再清理构建和 worktree：

```bash
git worktree remove baseline-main
rm -rf build-accept-gdal build-accept-nogdal \
       build-consumer-nogdal build-consumer-legacy build-consumer-gdal \
       build-perf-current build-perf-main build-writer-profile \
       install-accept-nogdal install-accept-gdal install-perf-current
```

不要删除 `local-acceptance/` 证据，直到 PR #13 最终审核完成。