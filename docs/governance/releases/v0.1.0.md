> 状态：Current
> 适用版本：v0.2.0+
> 维护入口：docs/governance/

# fast-gdb v0.1.0

fast-gdb 的首个能力范围正式版本，提供 C++17 FileGDB Reader、统一几何模型、标准 WKB 输出以及可选 GDAL Hybrid 回退。

## 主要能力

- 无 GDAL 依赖的 `fast_gdb::linear` 产品；
- 可选 GDAL 回退的 `fast_gdb::hybrid` 产品；
- Point、MultiPoint、Polyline、Polygon；
- multipart、洞、岛中岛、Z、M、ZM；
- CircularArc、CubicBezier、EllipticArc 内置线性化；
/

- ArcGIS/FileGDB `0x42` 缺失-M 数组编码兼容；
- ISO WKB-first 输出；
- 显式 FID 映射和 `.spx` 辅助空间过滤；
- 可重定位 CMake package 和 CPack 归档。

## 真实数据门禁

/

上传的 `testcurve.gdb.zip` 已与 GDAL/OpenFileGDB 对照：

/

- `Curve_Polyline_M_FC` 2/2 要素全部由 `BuiltinCurve` 解析；
/

- 0 次 Hybrid/GDAL fallback；
- FID 映射、WKB 类型、bbox、长度和整层空间查询全部通过；
- Release 与 ASan/UBSan/LSan 真实数据复验均为 0 失败。

## 发布资产

- Windows x64 纯 C++ ZIP；
- Linux x64 纯 C++ TGZ；
- macOS 纯 C++ TGZ；
/

- Linux x64 Hybrid TGZ，链接/运行时需要 GDAL；
- 所有归档的 `SHA256SUMS.txt`。

## Windows 验收结果 (2026-07-30)

Windows 平台 Reader-only 产品代码验收通过，0 个产品代码缺陷。

| 指标 | 值 |
|------|-----|
| 唯一测试总数 | 447 |
| 通过 | 433 (96.87%) |
| 预期跳过 | 12 (2.68%) |
| 环境限制 | 1 (0.22%) |
| 产品代码缺陷 | 0 |

### 通过项

- CMake 编译 (linear/hybrid/adaptive) + 包消费者
- 元数据解析 (Domain, Relationship, FD, SRS)，使用 `acceptance_metadata.gdb`
- 404 字段期望值、NULL、Binary、DateTime、UUID、稀疏 FID
/

- 几何核心 101/101 通过
- 损坏输入 fail-closed
/

- GDAL 写入 → 关闭 → 重开 Reader 10/10 通过
/

- 自适应 Reader 协调器 40/41 通过 (1 预期跳过)
- 大型空间索引解析 (2M 要素 .spx)
- 独立 2/4/8 线程 Reader 并发一致性

### 已知限制

- `ReaderContractTest.OpensLayerAndAppliesCursorOptions` 因 GDAL 生成数据缺少系统目录 XML 元数据而受环境限制（ArcGIS 生成数据上通过）
/

- 2M 要素空间索引结果不等同于 10M/50M 极限性能验收
- 曲线几何测试因缺少 `testcurve.gdb` (需 ArcGIS Pro arcpy) 而跳过

完整证据: `release-evidence/2026-07-30-win-acceptance/`

## 能力边界

- 曲线以线性化标准 WKB 输出，不保留 ArcGIS 原生 curve object；
- MultiPatch 仅提供 Hybrid degraded support；
- Writer 仍为实验性组件，不在 v0.1.0 生产支持声明内；
- 未知或未来 FileGDB 几何编码不在本版本承诺范围。

完整证据见：

- 历史验收证据已移除；发布说明以当前测试文档为准。
