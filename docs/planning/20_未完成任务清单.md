# Reader-only 状态与后续任务

本文只记录当前 Reader 产品边界内的状态。`[x]` 表示本地代码和测试已有证据，
不表示 Linux、Windows 或其它 GDAL 版本已经验收；`[ ]` 表示仍需补证据或补能力。

## 已实现并有本地证据

- [x] `Reader → Layer → Query/Cursor` 公共只读入口，以及结构化 `ReaderError`、
  `QueryResult` 和 `MetadataReadResult`。
- [x] FID、顺序、空间、属性、WHERE、SpatialWhere、投影、分页/限制、取消和
  单活动 Cursor 合同；当前公共 FID 是零基 `uint32_t`，超出范围时 fail closed。
- [x] `Layer::read_metadata()` 统一返回元数据快照和结构化错误；
  `metadata_snapshot()` 保留为兼容入口。
- [x] 独立 Reader/Layer/QueryEngine 对象图可并发；共享同一对象图仍明确不支持。
  2/4/8 线程 digest 覆盖 FID、字段值、NULL、WKB、查询和 Cursor。
- [x] Adaptive 同进程 `WriterPending → drain → WriterActive → close → generation+1`、
  Busy、租约排空、旧 Cursor 过期、token 丢失/关闭失败和 fresh GDAL 未验证标记。
- [x] fast query 读取前后 generation 诊断；generation 或 source verification 不一致时
  丢弃物化结果。已获租约的查询可完成，但记录 `writer_pending_seen`。
- [x] GDAL 写后完整重开门禁：Set/Create/Delete Feature、Create/DeleteField、属性索引、
  extent、REPACK；重叠读写只做 old/new/mixed/error characterization。
- [x] `.gdbtable`、`.gdbtablx`、`.spx`、`.gdbindexes` 截断和损坏系统目录 fail closed；
  `.atx` 错误链、几何/WKB 和字段类型已有独立安全测试。
- [x] 安装目标为 `linear`、可选 `hybrid` 和可选 `adaptive`；`linear` 不依赖 GDAL，
  安装面不导出 Writer。

### 真实 ArcGIS Pro 验收数据（2026-07-29）

`test_data/gdb/acceptance_metadata.gdb` 已生成，但首次运行暴露出一个真实兼容性缺口：
`all_field_types` 在 Binary 字段后无法继续解析字段。原因是 ArcGIS Pro 的 Binary
字段描述包含 `width + flag` 两个字节，旧代码只消费了 `flag`，造成后续字段描述错位。
该缺口已修复，并已由真实数据回归测试覆盖。`all_field_types` 的 BigInteger、DateOnly、
TimeOnly、TimestampOffset 已完成物理读取验证；这不等同于所有历史/未来 ArcGIS 版本均已验收。

## 本地仍可补强

- [x] source safe-point 检查复用已捕获的文件快照进行直接比对，不再为每次检查构造临时
  全目录快照；仍校验文件名、大小、修改时间和文件数量，未弱化源变化 fail-closed 合同。
- [x] Polygon 拓扑方向和线段关系补充 `int64_t` 全范围、负极值乘积、相交与重叠边界测试；
  当前实现已使用精确宽度的中间计算，不再把该项记录为已确认的整数溢出缺陷。
- [ ] `.atx` 大文件 direct query 的分块读取/mmap 仍需真实大索引的峰值 RSS 和查询基准；
  当前 direct query 已避免 `all_entries_` 全量物化，但默认仍完整加载索引文件。
- [ ] nullable bitmap 候选布局和 `.atx` direct query 需要稳定的大数据 benchmark；没有该证据前，
  不将有限布局探测描述为 O(n³)，也不替换为未经验证的单一布局。
- [ ] metadata 损坏 XML、关系/域/Feature Dataset 的更细粒度错误码和 conformance
  fixture；公开快照已补充 workspace domains、完整 relationship definitions，以及
  系统表 `ok/empty/missing/unreadable` 审计。XML 结构级损坏仍需真实语料补强。
- [x] 字段描述符默认值不再被跳过，原始字节通过 `FieldDescriptor::default_value_raw`
  保留；按类型的规范化默认值解码仍需真实 ArcGIS 数据验证。
- [x] 元数据快照增加系统表审计和 Subtype code/name/default-value 结构化入口；每字段
  Subtype 默认值、域绑定和 ArcGIS 版本差异仍需 Windows 原生语料验证。
- [ ] 显式空间索引删除/重建和更多 GDAL 驱动能力差异的 characterization；当前
  OpenFileGDB DeleteIndex 不支持时明确记录 `SKIPPED`，不把驱动能力差异误判为 Reader 失败。
- [ ] NULL、Binary、DateTime、UUID、BigInteger、DateOnly、TimeOnly、TimestampOffset、
  零长度记录、稀疏/删除 FID 的真实数据统一 digest；sidecar 的 404 条逐字段记录已完成
  定位、NULL、字符串、整数、浮点、日期、时间、时区和 UUID 规范化值门禁；Binary 仍只
  验证 payload 存在，待 sidecar 提供稳定十六进制摘要后再做字节级比较。
- [x] `acceptance_metadata.gdb` 的 Domain、Relationship Class、Feature Dataset 层级和
  空间参考已通过真实 ArcGIS Pro 系统表结构化读取门禁；跨版本语义仍需外部语料验收。
- [ ] 曲线线性化误差、MultiPatch degraded WKB、栅格占位和资源预算的公开诊断字段。
- [ ] 线程安全说明、Cursor 生命周期、源快照 safe-point 和 Adaptive 路由的示例代码。
- [ ] ASan/UBSan/TSan 的可重复构建命令和产物归档；未执行的平台必须标记 `SKIPPED`。

### 本机补充证据（macOS，GDAL 3.13.0）

- [x] `linear` GDAL OFF 安装 consumer configure/build/CTest 通过。
- [x] `hybrid` 和 `adaptive` 安装 consumer configure/build/CTest 通过。
- [x] ASan/UBSan 构建下，独立 Reader 并发和 ArcGIS Pro 真实元数据门禁通过；曲线数据因未提供而 `SKIPPED`。
- [ ] TSan、其它平台和其它 GDAL 版本仍需独立环境，不能由本机结果代替。

## 跨平台和发布验收

- [ ] Linux、Windows、macOS 三平台独立构建、安装 consumer 和完整测试证据。
- [ ] 多个稳定 GDAL 版本的 OpenFileGDB 创建、更新、索引、REPACK 和只读重开矩阵。
- [ ] 冷/热打开、Tablx 全局缓存竞争、不同图层并行和反复打开关闭的外部证据。
- [ ] 损坏文件、异常几何和真实 ArcGIS/GDAL 语料库的发布 manifest 与可复现日志。
- [ ] 10M、峰值 RSS、p95/p99 和吞吐基线；这些不作为本轮合同收口的前置条件。

## 明确非目标

- fast-gdb Writer、自研事务、Recovery、VersionedGdbStore 或在线副本切换。
- 对未协调外部 Writer 的强安全检测、强一致性或自动路由；文件快照和 `.lock`
  只能是 best-effort 诊断。
- 共享 Reader/QueryEngine 的线程安全语义或 Feature 热路径全局锁。
- 完整栅格读取、完整 MultiPatch 表面拓扑和将 degraded 结果伪装为完整支持。

以上非目标保留为边界记录，不作为待实现功能。FileGDB 编辑继续由
GDAL/OpenFileGDB 或业务服务层承担。
