# 未完成任务清单

## P0：Reader-only 架构闭环

- [ ] 三平台确认安装包不导出 Writer target 或 Writer 头；
- [ ] CMake/CTest 完整日志和 artifact 可用；
- [ ] package consumer 只验证 linear/hybrid；
- [ ] 删除后搜索确认无 VersionedGdbStore、WriterSession、Append/Update/Delete 公共引用；
- [ ] 所有主文档保持 Reader-only 口径。

## P0：GDAL 写后 Reader 重开

- [ ] `SetFeature` 正式门禁；
- [ ] `CreateFeature`；
- [ ] `DeleteFeature`；
- [ ] `CreateField/DeleteField`；
- [ ] 属性索引创建和删除；
- [ ] 空间索引重建；
- [ ] `REPACK`；
- [ ] extent 重算；
- [ ] Windows/Linux/macOS；
- [ ] 多个 GDAL 稳定版本。

## P0：同目录重叠风险观测

- [ ] 记录已有 Reader 在 GDAL Writer 打开期间的 old/new/mixed/error；
- [ ] 记录新 Reader 在 Writer 打开期间的行为；
- [ ] 记录 GDALClose 后旧 Reader 的行为；
- [ ] 大表、索引、几何更新和 REPACK 压力观测；
- [ ] 文档明确观测结果不构成支持声明。

## P1：Reader 正确性

- [ ] 稀疏和删除 FID；
- [ ] 零长度记录；
- [ ] NULL、Binary、DateTimeWithOffset；
- [ ] `.spx/.atx` 损坏回退；
- [ ] 复杂 Polygon、曲线和 MultiPatch profile；
- [ ] 关系、域、subtype、feature dataset 和栅格边界。

## P1：性能

- [ ] 100K/1M 顺序扫描；
- [ ] FID lookup；
- [ ] SpatialWhere 选择性矩阵；
- [ ] Cursor vs record+geometry vs GDAL；
- [ ] mmap/fd 路径；
- [ ] 内存和 p95/p99；
- [ ] 多线程独立 Reader。

## P1：生命周期

- [ ] 活动 cursor 阻止 engine 重开；
- [ ] engine move 后 cursor 控制状态；
- [ ] 外部写入前 drain 示例；
- [ ] 服务状态机示例；
- [ ] 写后完整对象图重建；
- [ ] 禁止局部 refresh 的审核规则。

## P2：业务层在线更新参考

该能力不进入 fast-gdb 核心，但可提供独立参考文档：

- [ ] 双副本目录布局；
- [ ] 外部验证；
- [ ] 业务路由切换；
- [ ] 进程间锁；
- [ ] 旧副本回收；
- [ ] 崩溃恢复。

任何实现若进入仓库，必须独立 ADR 和独立 target，不能重新引入 FileGDB Writer。
