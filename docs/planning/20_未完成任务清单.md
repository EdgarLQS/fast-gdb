# 20 — M18 与 Reader 后续未完成任务清单

- **更新日期**：2026-07-17
- **执行分支**：`codex/m18-main-closeout`
- **关联计划**：[19_M18正式收口与Reader性能优化计划.md](19_M18正式收口与Reader性能优化计划.md)
- **当前总判定**：`Code accepted / Formal acceptance blocked`
- **合并状态**：PR #13 保持 Draft；本清单全部达到相应完成判据前不得标记计划 `Accepted`

本文集中记录当前尚未完成的任务。已完成的文档同步、Reader 回归工具扩展和静态测试不在此重复列出。

## 1. 外部基础设施阻塞

### U-001 GitHub Actions 恢复

- **状态**：Blocked
- **跟踪入口**：Issue #12
- **当前现象**：PR #13 触发的 jobs 在 checkout 前结束，返回 `steps=None`，无可下载 job log，无 artifact。
- **需要完成**：确认仓库、账户或 runner 配额/授权/队列问题，使 workflow 至少能够执行 checkout、configure、build、test 和 upload-artifact。
- **完成判据**：同一当前 SHA 上至少一个既有 workflow 出现实际 steps 和可读取日志；随后重新执行下列 Writer 与 Reader 门禁。

## 2. M18 Writer 正式验收

### U-101 GDAL ON Release 与完整 CTest

- **状态**：Not run on current SHA
- **要求**：Release 配置、完整构建、完整 CTest。
- **完成判据**：配置、编译、链接和所有 required tests PASS；记录命令、依赖版本、SHA、日志和 SKIP 原因。

### U-102 GDAL OFF Release 与完整 CTest

- **状态**：Not run on current SHA
- **要求**：无 GDAL Release 配置、完整构建、完整 CTest。
- **完成判据**：无 GDAL 稳定安装面可编译运行，所有 required tests PASS。

### U-103 五套 Writer 功能合同连续三次

需要分别运行：

1. Writer 基础合同；
2. Append 合同；
3. Update 合同；
4. Delete 合同；
5. Transaction/Recovery 合同。

- **状态**：No current-SHA runtime evidence
- **完成判据**：每套合同连续三次结果一致；required 场景全部 PASS；所有 SKIP 均有结构化原因且不计入通过数。

### U-104 Writer 性能合同

- **固定参数**：`samples=3`、`profile_duration_seconds=5`、`run_reader_10m=false`
- **状态**：No current-SHA artifact
- **完成判据**：正确性先通过；current/main/GDAL 输出完整；取得 raw profile；性能结论绑定环境、数据和 SHA。

### U-105 三类 package consumer

需要验证：

- no-GDAL consumer；
- legacy consumer；
- GDAL consumer，覆盖 Index、Append、Transaction 等批准入口。

- **状态**：No current-SHA runtime evidence
- **完成判据**：独立安装、`find_package`、编译、链接和实际运行全部通过。

### U-106 六个 macOS artifacts

需要取得绑定当前 SHA 的：

1. `writer-macos-contract-<sha>`；
2. `writer-append-macos-<sha>`；
3. `writer-update-macos-<sha>`；
4. `writer-delete-macos-<sha>`；
5. `writer-transaction-macos-<sha>`；
6. `writer-macos-performance-<sha>`。

- **状态**：Blocked by U-001
- **完成判据**：每个 workflow 实际执行 checkout、Release 构建、合同/consumer 和 artifact 上传；artifact 内容可下载并审计。

### U-107 故障注入与 Recovery 边界

至少覆盖：

- publish、rollback、cleanup 失败；
- source 损坏；
- backup 损坏；
- 伪造候选；
- 多候选歧义；
- 错误恢复动作；
- 发布后重开验证失败。

- **状态**：No current-SHA runtime artifact
- **完成判据**：源数据安全、失败状态可诊断、Ambiguous 状态拒绝自动覆盖、恢复动作可审计和可重复。

## 3. Reader 10M fresh-open 性能

### U-201 准备可运行环境

- **状态**：Blocked
- **要求**：macOS Apple Silicon checkout、GDAL、CMake、编译器，以及 Point/MultiPoint/Polyline 三份 10M fixture。
- **完成判据**：能够构建 current 与固定 baseline `main@42d8f76`，并运行 `SpatialDensityBenchmark.DensityMatrixConfigured`。

### U-202 执行回归工具静态测试

- **状态**：Test code written, not executed in current environment
- **命令**：`python3 -m unittest tests/scripts/test_run_spatial_regression.py`
- **完成判据**：全部测试 PASS，记录 Python 版本和输出。

### U-203 三类 10M × 五档 fresh-open 基线

数据集：Point、MultiPoint、Polyline。

覆盖率：1%、10%、30%、80%、100%。

每档必须记录：

- fast-gdb median/p95；
- GDAL median/p95；
- fast/GDAL 比值；
- result count；
- 完整 FID 对照或稳定签名；
- `invalid_geometries`；
- execution path、候选数和分段耗时；
- 明确标记 `fresh-open，非 strict-cold`。

- **状态**：Not run on current branch
- **完成判据**：15 档均产生可审计 CSV、JSON、environment 和 raw logs；正确性全部通过。

### U-204 当前热点 profile

优先场景：

- Point：10%、30%、80%、100%；
- MultiPoint：10%、30%；
- Polyline：30%。

至少拆分：catalog、table/tablx open、`.spx` open/mmap、candidate lookup、FID 定位、blob 读取、decode、bbox/predicate、结果收集与排序去重。

- **状态**：Not run
- **完成判据**：热点可重复，且某阶段约占总耗时 20% 以上，才能进入算法修改。

### U-205 Profile 驱动的 Reader 优化

候选方向仅在 profile 支持时实施：

- Point 10%：候选复制、排序去重、tablx 定位、最小 XY 解析；
- Point 30%–100%：安全的 Point/PointZ/PointM/PointZM geometry-only 最小 XY 路径；
- MultiPoint/Polyline：通用 scan/decode/predicate 热点。

- **状态**：Not started by design
- **约束**：不得放宽阈值、跳过精确判断或隐藏 SKIP；未知、截断和非法编码必须完整回退或明确失败。
- **完成判据**：目标场景稳定改善至少 5%；完整 FID 一致；`invalid_geometries=0`；其他档位无超过 5% 回退。

### U-206 Reader 最终验证矩阵

执行顺序：

1. 目标场景快速测量 3 次；
2. 正式测量 5 次；
3. 同几何五档复测；
4. 三类几何完整正确性矩阵；
5. current/main 5% 回退门禁；
6. Reader 专项测试；
7. GDAL ON/OFF 完整 CTest。

- **状态**：Blocked by U-201/U-204/U-205
- **完成判据**：正确性无回退，性能门禁满足；保留与撤销的优化均记录原因。

## 4. 最终审核与合并

### U-301 最终证据汇总

- **状态**：Pending
- **要求**：同时汇总 Writer 正式证据、Reader 15 档 current/main/GDAL、profile、优化取舍、SKIP、Deferred、预存问题和未覆盖项。
- **完成判据**：所有结论均能追溯到当前 SHA 的命令、日志或 artifact。

### U-302 `main...HEAD` 最终 P0/P1/P2 审核

- **状态**：需在运行结果和最终代码确定后重新执行
- **完成判据**：P0/P1 无阻塞问题；P2 已处理或明确接受；没有测试数据、构建目录、raw profile 或临时输出误提交。

### U-303 PR #13 最终合并

- **状态**：Draft / Do not merge
- **前置条件**：U-001 至 U-302 中属于当前计划范围的任务全部满足完成判据。
- **合并判定**：
  - `Accepted`：本地验证和正式 artifacts 全部齐全；
  - `Code accepted / Formal acceptance blocked`：不得合并为正式完成；
  - `Rejected`：存在正确性、恢复安全、构建、安装消费或不可接受性能回退。

## 5. Deferred，不计入本计划完成率

以下事项继续 Deferred，不能从当前 macOS 或 10M 结果外推：

- Linux Writer 正式矩阵；
- Windows Writer 正式矩阵；
- Linux/Windows Reader 绝对性能比较；
- 50M 阶梯；
- 35GB/5 亿级生产数据；
- 并发 Writer 与发布窗口连续 Reader；
- 嵌套事务、savepoint、跨 GDB/跨层事务；
- 原生曲线写入；
- MultiPatch 完整写入与拓扑。

## 6. 状态维护规则

- 任务完成后，将状态改为 `Completed`，并补充证据路径、运行 SHA、命令及结果。
- 任务因外部条件无法执行时保持 `Blocked`，不得改写为 PASS。
- `SKIP` 必须附具体原因，不得计入完成数量。
- 历史测试结果只能作为背景，不能替代当前分支/当前 SHA 的验收证据。
