# 21 — 空间属性联合查询与完整 Feature 流式迭代计划

- **更新日期**：2026-07-17
- **分支**：`codex/spatial-attribute-query`
- **基线**：`main@d8784e7`
- **参考提交**：`4425a8485b4ee28265e2e6c25996c6322090cfce`
- **联合查询**：实现与静态自检完成
- **FeatureCursor**：实现与静态自检完成
- **当前阶段**：**Code review ready**
- **正式验收**：**Formal acceptance blocked**

审核入口：

- [代码审核指南](../usage/10_空间属性联合查询代码审核指南.md)
- [FeatureCursor 自检](../evidence/feature-cursor-self-review-2026-07-17.md)
- [联合查询自检](../evidence/spatial-attribute-query-self-review-2026-07-17.md)
- [覆盖矩阵](../usage/04_功能与基准测试覆盖矩阵.md)
- [Reader 流程](../../src/edgar/explorgdb/reader/QUERY_FLOW.md)

## 1. 目标

本分支保留 FID-only 查询，同时新增完整对象流式读取：

```cpp
QueryResult QueryEngine::query(const QueryRequest& request);
FeatureCursor QueryEngine::open_cursor(const QueryRequest& request);
```

目标：

1. `SpatialWhere` 等于精确空间结果与完整 WHERE 结果的交集；
2. `.spx/.atx` 只提供候选，不能替代最终判断；
3. `query()` 继续返回升序、唯一、零基 FID；
4. cursor 每次只物化一个完整 Feature；
5. SequentialScan 不先物化全表 FID；
6. EOF 与失败明确区分；
7. `move_to(fid)` 支持前后和跳跃；
8. 旧查询、安装面和 Reader 字段语义保持兼容。

## 2. 公开接口

```cpp
struct QueryFeature {
    uint32_t fid = 0;
    FeatureRecord record;
    GeometryValue geometry;
};

class FeatureCursor {
public:
    FeatureCursor(FeatureCursor&&) noexcept;
    FeatureCursor& operator=(FeatureCursor&&) noexcept;
    FeatureCursor(const FeatureCursor&) = delete;
    FeatureCursor& operator=(const FeatureCursor&) = delete;
    ~FeatureCursor();

    bool next(QueryFeature& feature);
    bool move_to(uint32_t fid);
    bool done() const noexcept;
    const QueryResult& query_result() const noexcept;
    const std::string& error() const noexcept;
};
```

`QueryEngine` 不可复制或移动，避免 cursor 保存的 engine 地址失效。

## 3. 联合查询

流程：

1. 校验 bbox 和 WHERE；
2. `query_bbox_unified()` 得到精确空间命中；
3. 单个安全比较解析 `.gdbindexes` 和 `.atx`；
4. 属性候选排序、去重；
5. 与精确空间 FID 线性交集；
6. 对交集执行完整 WHERE；
7. 缺失、损坏或不安全索引回退；
8. 最终 FID 排序、去重并记录指标。

执行路径：

- `spatial-where:spx+atx`；
- `spatial-where:spatial-candidates`；
- `spatial-where:sequential`；
- `spatial-where:invalid`。

安全回退包括非 BMP、`!=`、函数索引、不明确数值编码和任意 `.atx` 结构损坏。

## 4. Cursor 执行模式

### 4.1 候选 FID 模式

适用于 ReadByFid、SpatialBbox、两类属性查询、WhereClause 和 SpatialWhere：

- 调用既有 `query(request)`；
- 保存 execution path、fallback 和 metrics；
- FID 排序、去重；
- 清除 ReadByFid 的重复完整 record；
- 按 FID 逐条读取完整对象。

只保存 FID vector，不保存完整 Feature vector。

### 4.2 SequentialScan 流模式

- 不调用全表 `query_sequential_scan()`；
- 只保存 feature limit 和下一个槽位；
- 用 `.gdbtablx` `has_feature()` 跳过删除槽；
- 每次 `next()` 只构造一条完整对象；
- `query_result().matched_fids` 保持为空。

## 5. `move_to(fid)`

参数是 fast-gdb 零基 FID，不是结果序号。

```text
move_to(target)
  -> 下一次 next() 返回当前查询结果中第一个 FID >= target 的对象
```

- 支持向前、向后、rewind 和任意跳跃；
- 候选模式使用 `lower_bound`；
- 顺序模式从 target 槽开始并跳过删除槽；
- target 不匹配时跳到下一个匹配结果；
- target 后无结果时进入正常 Exhausted；
- Exhausted cursor 可在 engine 未改变且无其他 cursor 活动时重新定位；
- Failed、moved-from 或 engine 已重开时不能重新定位；
- reacquire 必须在任何 parser/tablx 访问前完成。

## 6. 完整对象语义

`next()` 使用局部 candidate，只有全部成功后才覆盖输出：

1. `read_record_by_fid()`；
2. 校验 `record.fid` 和字段数量；
3. 规范化合法零长度行；
4. `read_geometry_value()`；
5. move 到调用方对象。

支持：

- ObjectID、标量、String/XML；
- NULL 和 nullable bitmap；
- Binary 原始字节；
- DateTimeWithOffset 既有表示；
- Geometry 兼容 WKT 和规范化 ISO WKB；
- 无几何表：成功 `UnsupportedType`；
- NULL/Empty geometry：成功 `Empty`；
- ObjectID-only 零长度行：生成 ObjectID 和 nullable NULL；
- 损坏或无法恢复的非空对象：Failed。

## 7. 生命周期和状态

| 状态 | `next()` | `done()` | `error()` |
|---|---|---:|---|
| Ready | 下一条或转 EOF/Failed | false | 空 |
| Exhausted | false | true | 空 |
| Failed | false | false | 首次错误 |
| MovedFrom | false | true | 空 |

规则：

- 同一 engine 同时一个活动 cursor；
- cursor generation 防迟到析构；
- open generation 防 engine 重开后旧计划复用；
- `open()` 重置 parser、空间索引缓存和 capability 状态；
- capability 判定失败时不保留可用 parser；
- 活动 cursor 期间 engine 读入口拒绝；
- QueryEngine、Catalog 和 ResolvedTable 必须比 cursor 活得更久；
- 不声明同一 QueryEngine 线程安全。

## 8. 测试代码

### GDAL OFF

- `test_feature_cursor.cpp`：move-only cursor、non-movable engine、接口签名；
- 既有 WHERE、索引表达式和 `.atx` fail-closed 测试。

### GDAL ON

- `test_feature_cursor_gdal.cpp`：顺序流、所有候选 QueryKind、字段/Binary/WKB、
  move ownership、engine guard、前后和跳跃 `move_to`；
- `test_feature_cursor_empty_geometry.cpp`：NULL geometry；
- `test_feature_cursor_zero_length.cpp`：ObjectID-only 行；
- `test_feature_cursor_reopen.cpp`：open generation 和 reacquire；
- `test_feature_cursor_benchmark.cpp`：100K full-feature cursor/legacy/GDAL。

联合查询测试继续覆盖几何、Z/M/ZM、NULL、Unicode、函数索引和索引故障。

## 9. Package consumer

安装后 consumer 编译：

- `<query_engine.h>`；
- `QueryFeature`；
- `QueryEngine::open_cursor`；
- `FeatureCursor::next/move_to/done/error`；
- move-only cursor；
- 不依赖内部 WHERE 类型。

## 10. 性能

FID-only runner 保持现有联合查询语义。

Full-feature runner 显式开启：

```bash
FAST_GDB_RUN_FEATURE_CURSOR_BENCHMARKS=1 \
FAST_GDB_BENCHMARK_OUTPUT_DIR=<external-dir> \
ctest --test-dir build-on --output-on-failure \
  -R FeatureCursorBenchmarkTest.Point100KFullFeatureEvidence
```

每个样本分别打开 cursor engine、legacy engine 和 GDAL dataset，计时范围为：

```text
engine-or-dataset-open-through-last-feature
```

三方进入同一 checksum sink，比较 FID、Int32、Binary、ISO WKB、count 和 output bytes。

仍需 peak RSS、100K 选择性矩阵、10M 多几何、current/main/GDAL 交替和 5% 门禁。

## 11. 三轮静态自检

FeatureCursor 自检结果：

- P0：1 项，空间 planner 意外覆盖已恢复；
- P1：10 项，生命周期、打开状态、兼容、测试 API、零长度行和 benchmark 范围已修复；
- P2：0 项。

证据：`docs/evidence/feature-cursor-self-review-2026-07-17.md`。

联合查询早期自检证据继续保留。

## 12. 当前未完成证据

- [ ] GDAL OFF/ON Release；
- [ ] 完整和并行 CTest；
- [ ] package consumer；
- [ ] FeatureCursor 功能测试实际通过；
- [ ] GDAL 完整对象对等实际通过；
- [ ] 100K full-feature benchmark；
- [ ] peak RSS；
- [ ] 10M full-feature；
- [ ] current/main 和 5% 门禁；
- [ ] `git diff --check main...HEAD`。

## 13. 验收判定

- **Blocked**：错误被当 EOF、`move_to` 返回错误 FID、完整对象与 GDAL 不一致、
  生命周期破坏 engine、顺序流物化全表、旧查询回归、损坏索引假空集或安装面泄露；
- **Fix then re-review**：独立审核发现 P0/P1；
- **Ready for formal validation**：代码审核无阻断项但证据未齐；
- **Accepted**：构建、CTest、consumer、完整对象对等、100K/10M、peak RSS 和门禁全通过。
