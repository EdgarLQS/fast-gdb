# 00 — fast-gdb 规划文档状态索引

**更新日期**：2026-07-18
**当前分支**：`codex/spatial-attribute-query`
**分支基线**：`main@d8784e7`
**参考提交**：`acb9478670ae7fbb7cce2322abe6583cee7661db`
**当前工作**：SpatialWhere、WKB-first Reader、按需 WKT、FeatureCursor 与四批注释审计
**当前判定**：**Local review passed / Formal acceptance blocked**

本文档是当前分支的审核状态入口。分支能力尚未进入 `main`；实现完成、测试源码完成和
局部离线检查不得解释为完整构建、性能或发布验收通过。

## 1. 当前审核入口

| 顺序 | 文件 | 角色 | 当前状态 |
|---:|---|---|:---:|
| 1 | [21_空间属性联合查询实现计划.md](21_空间属性联合查询实现计划.md) | 联合查询、FeatureCursor、`move_to`、测试和验收清单 | 本地审核通过 |
| 2 | [22_Reader_WKB-first与按需WKT计划.md](22_Reader_WKB-first与按需WKT计划.md) | WKB-first、record Geometry 空占位、按需 WKT | 本地审核通过 |
| 3 | [注释计划](../plan/注释计划.md) | common、reader、writer、usegdal 职责与关键设计审计 | 四批完成，待完整构建 |
| 4 | [空间属性联合查询代码审核指南](../usage/10_空间属性联合查询代码审核指南.md) | 审核顺序、P0/P1 检查点和建议命令 | 当前审核入口 |
| 5 | [FeatureCursor 三轮自检](../evidence/feature-cursor-self-review-2026-07-17.md) | 生命周期、完整对象、兼容和构建表面问题 | 当前证据 |
| 6 | [联合查询三轮自检](../evidence/spatial-attribute-query-self-review-2026-07-17.md) | WHERE、索引、回退和并行测试问题 | 当前证据 |
| 7 | [功能与基准测试覆盖矩阵](../usage/04_功能与基准测试覆盖矩阵.md) | 功能、自动化、性能和 CI 状态 | 当前矩阵 |
| 8 | [Reader 查询流程](../../src/edgar/explorgdb/reader/QUERY_FLOW.md) | FID-only、WKB-first full-feature 和按需 WKT | 技术入口 |
| 9 | [2026-07-18 审核与验证证据](../evidence/branch-review-and-validation-2026-07-18.md) | 修复、构建、CTest、consumer 和 100K 结果 | 本地提交门禁 |

## 2. 当前实现进度

| 工作项 | 状态 | 说明 |
|---|:---:|---|
| `QueryKind::SpatialWhere` | 完成 | bbox + WHERE 单次联合查询 |
| WHERE parser/evaluator | 完成 | 内部实现，不进入安装包 |
| `.spx + .atx` 候选交集 | 完成 | 最终精确几何和完整 WHERE 复核 |
| `.atx` fail-closed | 完成 | 页面、页链、计数、FID 和越界损坏均回退 |
| `QueryFeature` / move-only `FeatureCursor` | 完成 | 完整普通字段和独立 GeometryValue |
| SequentialScan 真流模式与 `move_to(fid)` | 完成 | 不先物化全表对象；支持 rewind/跳跃 |
| 单活动 cursor 守卫 | 完成 | cursor generation + open generation |
| `read_record_by_fid()` WKB-first | 完成 | 验证并跳过 Geometry blob，不解码几何 |
| `parse_records()` WKB-first | 完成 | 批量 records 复用单条 record 契约 |
| `read_feature_by_fid()` WKB-first | 完成 | 一次字段物化、一次解码、一次 WKB |
| Geometry 空占位 | 完成 | record/feature 的 Geometry 槽统一 `std::string{}` |
| `GeometryValue::to_wkt()` | 完成 | 纯 C++、大小端、Z/M/ZM、严格 fail-closed |
| WKT 指标删除 | 完成 | FeatureRead/Cursor/benchmark 不再含 eager WKT 阶段 |
| package consumer 契约源码 | 完成 | 安装头引用 `to_wkt()` 和 cursor API |
| schema-v3 100K benchmark | 完成 | Cursor、record+geometry、GDAL、显式 to_wkt |
| common/reader/writer/usegdal 注释审计 | 完成 | 解释职责、生命周期、索引安全和事务边界 |

## 3. 已取得的本地验证

- GDAL OFF Release 构建与并行 CTest：310/310 通过；
- GDAL ON Release 构建与并行 CTest：653/653 通过；
- GDAL OFF/ON 安装后的 package consumer 编译、链接和运行通过；
- FeatureCursor schema-v3 与 SpatialWhere schema-v2 100K runner 均 `correct=true`；
- 按需 WKT 覆盖大小端、Z/M/ZM、EMPTY、Multi 内 EMPTY 和损坏输入；
- `git diff --check` 在本轮修复后通过。

## 4. 尚未完成的正式发布证据

以下项目没有有效运行证据，因此不能标记 Accepted：

- peak RSS 与 10M full-feature benchmark；
- current/main 交替采样和性能门禁；
- Windows/Linux CI 和正式 artifact；
- 配置真实数据路径后的 release contract；
- 最新远端 `main` 上的服务器侧合并验证。

2026-07-18 最新 Actions 仍在创建任何执行 step 前结束：release job 的 `steps` 和
`logs_url` 均为空，后续 package/publish job 被跳过。该状态不能解释为代码失败或通过。

```text
Local review passed
Formal acceptance blocked
```

## 5. 维护原则

- 分支实现完成不等于进入 `main`；
- 测试源码存在不等于实际通过；
- `SKIPPED` 不计入通过；
- `.spx/.atx` 只提供候选，最终结果必须复核；
- record Geometry 空字符串只是占位，不表示 NULL/Empty；
- 几何状态只读取 `GeometryValue::status`；
- cursor false 返回后检查 `done()` 与 `error()`；
- `move_to` 按零基 FID 定位，不按结果序号；
- QueryEngine、Catalog 和 ResolvedTable 必须比 cursor 活得更久；
- 性能结论必须绑定 SHA、数据、平台、缓存状态、计时范围和原始证据。
