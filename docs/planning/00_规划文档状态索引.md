# 00 — fast-gdb 规划文档状态索引

**更新日期**：2026-07-17
**当前分支**：`codex/spatial-attribute-query`
**分支基线**：`main@d8784e7`
**当前工作**：空间与属性联合查询实现、测试、三轮代码自审和文档自检
**当前判定**：**Code review ready / Formal acceptance blocked**

本文档是当前分支 `docs/planning/` 的状态入口。分支内实现、测试、计划和证据均保留在本分支，由维护者后续自行同步；不得把分支状态误写成已经进入 `main`。

## 1. 当前审核入口

| 顺序 | 文件 | 角色 | 当前状态 |
|---:|---|---|:---:|
| 1 | [21_空间属性联合查询实现计划.md](21_空间属性联合查询实现计划.md) | 实现范围、GDAL 参考、执行路径、测试矩阵、验收清单 | 当前计划 |
| 2 | [空间属性联合查询代码审核指南](../usage/10_空间属性联合查询代码审核指南.md) | 审核顺序、重点文件、P0/P1 检查点和建议命令 | 当前审核入口 |
| 3 | [三轮自我代码审核](../evidence/spatial-attribute-query-self-review-2026-07-17.md) | 三轮静态审核发现、修复提交和未运行项 | 当前证据 |
| 4 | [文档一致性自检](../evidence/spatial-attribute-query-document-audit-2026-07-17.md) | 文档冲突、修复范围和最终一致性结论 | 当前证据 |
| 5 | [功能与基准测试覆盖矩阵](../usage/04_功能与基准测试覆盖矩阵.md) | 功能、自动化、性能和 CI 状态 | 当前矩阵 |
| 6 | [Reader 查询流程图](../../src/edgar/explorgdb/reader/QUERY_FLOW.md) | `SpatialWhere` 实际执行链和回退语义 | 技术入口 |

## 2. 当前实现进度

| 工作项 | 状态 | 说明 |
|---|:---:|---|
| `QueryKind::SpatialWhere` 公共入口 | 完成 | 追加到枚举末尾，复用 bbox 与 `where_clause` |
| WHERE parser/evaluator 模块化 | 完成 | 内部头不进入安装包 |
| `.gdbindexes` 字段到 `.atx` 映射 | 完成 | 区分裸字段与函数索引 |
| `.spx + .atx` 候选交集 | 完成 | `.atx` 只提供候选，最终完整 WHERE 复核 |
| 稀疏候选字段扫描 | 完成 | mmap/fd 两条路径，失败时按 FID 回退 |
| `.atx` fail-closed | 完成 | 页链循环、页越界、计数不一致、FID 0 等均拒绝索引快速路径 |
| GDAL 等价测试代码 | 完成 | Point/Polyline/Polygon/MultiPoint、Z/M/ZM、NULL、Unicode、函数索引、索引故障 |
| package consumer 契约代码 | 完成 | 只使用公开 `<query_engine.h>` |
| 100K schema-v2 benchmark runner | 完成 | 默认跳过，记录实际执行路径和 correctness |
| 三轮代码自审 | 完成 | P0 2 项、P1 6 项、P2 1 项均已修复 |
| 文档自检与进度同步 | 完成 | 当前入口、矩阵、流程、测试索引和 Changelog 已同步 |

## 3. 尚未完成的正式证据

以下项目没有实际运行证据，因此不能标记 Accepted：

- GDAL ON Release；
- GDAL OFF Release；
- 完整 CTest；
- `ctest -j` 并行测试；
- package consumer；
- 100K 完整性能矩阵；
- 10M Point/MultiPoint/Polyline 性能矩阵；
- current/main 交替采样与 5% 回退门禁；
- `git diff --check main...HEAD`。

GitHub Actions 曾在创建任何 step 前结束，无法解释为代码失败或通过。正式状态保持：

```text
Code review ready
Formal acceptance blocked
```

## 4. 与其他当前文档的关系

- [01_项目状态与规划.md](01_项目状态与规划.md) 记录项目总体状态，同时把本分支联合查询标记为“分支实现完成、待审核和正式验证”。
- [02_GDAL功能对比矩阵.md](02_GDAL功能对比矩阵.md) 仍是 linear/hybrid/GDAL 产品边界，不因本分支代码自动改变发布声明。
- [18_writer跨平台测试统一与后续编辑计划.md](18_writer跨平台测试统一与后续编辑计划.md)、[19_M18正式收口与Reader性能优化计划.md](19_M18正式收口与Reader性能优化计划.md) 和 [20_未完成任务清单.md](20_未完成任务清单.md) 属于其他工作流，不是本分支联合查询的审核范围。
- 历史 Reader Phase A–H、Writer 阶段计划和发布报告仅作为背景，不替代本分支证据。

## 5. 维护原则

- 分支实现完成不等于已经进入 `main`；
- 测试代码存在不等于测试实际通过；
- `SKIPPED` 不计入通过数量；
- `.spx` 和 `.atx` 都只能提供候选，不能替代精确空间判断和完整 WHERE 复核；
- 损坏索引必须 fail closed，不得返回假空集；
- 性能结论必须绑定数据、平台、缓存状态、当前 SHA 和计时范围；
- 文档中的 `Completed` 仅表示实现或文档工作完成，正式验收必须附命令、日志或 artifact；
- 后续代码审核发现的问题应更新计划、审核指南和对应证据文档。
