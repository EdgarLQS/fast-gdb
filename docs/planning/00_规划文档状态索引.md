# 00 — fast-gdb 规划文档状态索引

**更新日期**：2026-07-18
**当前分支**：`codex/spatial-attribute-query`
**分支基线**：`main@d8784e7`
**参考提交**：`4425a8485b4ee28265e2e6c25996c6322090cfce`
**当前工作**：空间属性联合查询、完整 Feature 流式迭代、`move_to`、测试与自检
**当前判定**：**Code review ready / Formal acceptance blocked**

本文档是当前分支的审核状态入口。分支能力尚未进入 `main`；实现完成和测试代码完成
不得解释为实际构建、测试或发布验收通过。

## 1. 当前审核入口

| 顺序 | 文件 | 角色 | 当前状态 |
|---:|---|---|:---:|
| 1 | [21_空间属性联合查询实现计划.md](21_空间属性联合查询实现计划.md) | 联合查询、FeatureCursor、`move_to`、测试和验收清单 | 当前计划 |
| 2 | [22_Reader_WKB-first与按需WKT计划.md](22_Reader_WKB-first与按需WKT计划.md) | 全链路 WKB-first、record 几何空占位和按需 WKT | 待实现 |
| 3 | [空间属性联合查询代码审核指南](../usage/10_空间属性联合查询代码审核指南.md) | 审核顺序、P0/P1 检查点和建议命令 | 当前审核入口 |
| 4 | [FeatureCursor 三轮自检](../evidence/feature-cursor-self-review-2026-07-17.md) | 生命周期、完整对象、兼容和构建表面问题 | 当前证据 |
| 5 | [联合查询三轮自检](../evidence/spatial-attribute-query-self-review-2026-07-17.md) | WHERE、索引、回退和并行测试问题 | 当前证据 |
| 6 | [文档一致性自检](../evidence/spatial-attribute-query-document-audit-2026-07-17.md) | 文档冲突和修复范围 | 当前证据 |
| 7 | [功能与基准测试覆盖矩阵](../usage/04_功能与基准测试覆盖矩阵.md) | 功能、自动化、性能和 CI 状态 | 当前矩阵 |
| 8 | [Reader 查询流程](../../src/edgar/explorgdb/reader/QUERY_FLOW.md) | FID-only 与 full-feature 执行链 | 技术入口 |

## 2. 当前实现进度

| 工作项 | 状态 | 说明 |
|---|:---:|---|
| `QueryKind::SpatialWhere` | 完成 | bbox + WHERE 单次联合查询 |
| WHERE parser/evaluator | 完成 | 内部实现，不进入安装包 |
| `.spx + .atx` 候选交集 | 完成 | 最终精确几何和完整 WHERE 复核 |
| `.atx` fail-closed | 完成 | 页面、页链、计数、FID 和越界损坏均回退 |
| `QueryFeature` | 完成 | FID、完整记录和 GeometryValue |
| move-only `FeatureCursor` | 完成 | PImpl，不暴露 parser/FieldRef/WHERE 内部类型 |
| `QueryEngine::open_cursor()` | 完成 | 所有 QueryKind 共用入口 |
| SequentialScan 真正流模式 | 完成 | 不先物化全表 FID 或 Feature |
| `move_to(fid)` | 完成 | 前后、rewind、删除槽和任意 FID 跳跃 |
| EOF/Failed/moved-from | 完成 | `done()` 与 `error()` 明确区分 |
| 单活动 cursor 守卫 | 完成 | cursor generation + open generation |
| 完整字段和 WKB | 完成 | NULL、Binary、无几何、空几何、零长度行 |
| package consumer 契约代码 | 完成 | 公开头和 cursor 方法签名 |
| GDAL 完整对象对照代码 | 完成 | FID、字段、Binary 和 ISO WKB 顺序对照 |
| 100K full-feature runner | 完成 | 默认跳过，端到端打开范围，外部 evidence 输出 |
| 联合查询静态自检 | 完成 | P0 2、P1 6、P2 1 已处理 |
| FeatureCursor 静态自检 | 完成 | P0 1、P1 10 已处理 |

## 3. 尚未完成的正式证据

以下项目没有实际运行证据，因此不能标记 Accepted：

- GDAL OFF Release；
- GDAL ON Release；
- 完整 CTest；
- `ctest -j`；
- package consumer；
- FeatureCursor 运行时功能矩阵；
- GDAL 完整对象逐条对等；
- 100K full-feature benchmark；
- peak RSS；
- 10M Point/MultiPoint/Polyline full-feature benchmark；
- current/main 交替采样和 5% 回退门禁；
- `git diff --check main...HEAD`。

GitHub Actions 曾在创建任何 step 前结束，不能解释为代码失败或通过。

```text
Code review ready
Formal acceptance blocked
```

## 4. 与其他文档的关系

- [01_项目状态与规划.md](01_项目状态与规划.md) 记录项目总体状态；
- [02_GDAL功能对比矩阵.md](02_GDAL功能对比矩阵.md) 仍是正式产品边界，不自动加入本分支能力；
- M18 Writer、Reader fresh-open 性能和历史发布报告属于其他工作流；
- 根 README 和 v0.1.0 发布说明在本能力进入 main 并正式验收前保持既有范围。

## 5. 维护原则

- 分支实现完成不等于进入 `main`；
- 测试代码存在不等于实际通过；
- `SKIPPED` 不计入通过；
- `.spx/.atx` 都只提供候选；
- cursor false 返回后必须检查 `done()`；
- `move_to` 按零基 FID 定位，不按结果序号；
- QueryEngine、Catalog 和 ResolvedTable 必须比 cursor 活得更久；
- 性能结论必须绑定 SHA、数据、平台、缓存状态、计时范围和原始证据。
