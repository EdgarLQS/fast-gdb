# 01 — 项目状态与规划

**更新日期**：2026-07-17  
**文档状态**：当前分支权威状态  
**当前分支**：`codex/spatial-attribute-query`  
**分支基线**：`main@d8784e7`  
**参考提交**：`4425a8485b4ee28265e2e6c25996c6322090cfce`  
**状态入口**：[00_规划文档状态索引.md](00_规划文档状态索引.md)

## 1. 当前结论

| 模块/产物 | 状态 | 当前结论 |
|---|:---:|---|
| `usegdal` 高层组件 | ✅ | 既定 Phase 1A–3 已实现 |
| `fast_gdb_linear` | ✅ | 既有支持范围内的 Reader、统一几何模型、ISO WKB 和单类查询完成历史验收 |
| `fast_gdb_hybrid` | ✅ | 既有 fast-gdb 主路径和显式 GDAL 回退保持历史验收结论 |
| 空间查询 1K–10M | ✅/⚠️ | steady-state 历史矩阵通过；部分 10M fresh-open 性能档仍待独立优化 |
| 空间属性联合查询 | 🟡 | 当前分支实现和静态自检完成，待独立审核和正式运行证据 |
| 完整 Feature 流式迭代 | 🟡 | `QueryFeature`、`FeatureCursor`、`open_cursor()` 和 `move_to()` 已实现，待实际验证 |
| `explorgdb writer` | ✅/⚠️ | 既有 Writer 状态不由本分支改变 |
| MultiPatch | ⚠️ | 完整 part type 和表面拓扑仍不在当前范围 |

本分支新增能力尚未进入 `main`，不得扩展正式发布声明。当前状态统一为：

```text
Code review ready
Formal acceptance blocked
```

## 2. 当前发布边界

| 能力 | 状态 | 边界 |
|---|:---:|---|
| Point/MultiPoint/Polyline/Polygon、Z/M/ZM | ✅ | 既有读取、WKB 和空间判断范围保持不变 |
| CircularArc / Bezier / EllipticArc | ✅/⚠️ | 既有纯 C++ 线性化和 Hybrid 策略保持不变 |
| MultiPatch / GeneralMultiPatch | ⚠️ | 不保留完整 part type 和表面拓扑 |
| DateTimeWithOffset | ✅ | 值与 offset 独立暴露 |
| Raster 像素、Annotation、Dimension | ❌ | 不在本分支范围 |
| 完整 SQL / JOIN / 聚合 | ❌ | WHERE 仍是既有子集 |
| bbox + WHERE 联合查询 | 分支待审 | `SpatialWhere` 已实现，未进入正式发布范围 |
| full-feature cursor | 分支待审 | 完整字段 + GeometryValue 流式输出，未进入正式发布范围 |
| cursor `move_to(fid)` | 分支待审 | 按零基 FID 定位到首个 `FID >= target` 的当前查询结果 |
| 35GB/5 亿级性能 | 未验收 | 不能由 100K/10M 结果外推 |

## 3. 当前分支完成项

### 3.1 联合查询

- `QueryKind::SpatialWhere` 和 `CombinedQueryMetrics`；
- WHERE parser/evaluator 内部模块；
- `.gdbindexes` 到 `.atx` 映射；
- 裸字段、函数索引和未知表达式区分；
- `.spx + .atx` 有序候选交集；
- 最终完整 WHERE 复核；
- 稀疏候选字段扫描；
- `.atx` fail-closed；
- 几何、Z/M/ZM、NULL、Unicode、函数索引和故障测试代码；
- 100K FID-only benchmark runner。

### 3.2 完整 Feature 流式迭代

- `QueryFeature`；
- move-only `FeatureCursor` PImpl；
- `QueryEngine::open_cursor()`；
- 候选 FID 模式；
- 真正 SequentialScan 流模式；
- `next()` 完整字段和 GeometryValue；
- EOF、Failed 和 moved-from 区分；
- 单 engine 单活动 cursor；
- cursor generation 和 engine open generation；
- `move_to(fid)` 前后、rewind 和任意跳跃；
- 删除槽跳过；
- 无几何、NULL geometry 和 ObjectID-only 零长度行；
- package consumer 接口代码；
- GDAL 字段、Binary 和 ISO WKB 对照测试代码；
- 100K full-feature benchmark runner。

### 3.3 自检

- 联合查询早期三轮静态自检完成；
- FeatureCursor 生命周期、完整对象和兼容性三轮静态自检完成；
- 文档一致性入口已同步。

## 4. 当前未完成的实际证据

- GDAL OFF Release；
- GDAL ON Release；
- 完整 CTest；
- `ctest -j`；
- package consumer；
- FeatureCursor 所有 QueryKind 功能矩阵；
- GDAL 完整对象逐条对等；
- 100K full-feature benchmark；
- peak RSS；
- 10M Point/MultiPoint/Polyline full-feature benchmark；
- current/main 交替采样和 5% 回退门禁；
- `git diff --check main...HEAD`。

## 5. 当前后续事项

1. **独立代码审核**：重点检查 cursor 生命周期、完整对象、`move_to`、旧 API 兼容和空间 planner 未回归。
2. **正式功能验证**：执行 GDAL ON/OFF Release、完整和并行 CTest、consumer。
3. **完整对象性能**：运行 100K/10M full-feature、peak RSS 和 profile。
4. **Reader fresh-open 性能**：作为独立工作流处理历史性能失败档。
5. **生产规模和范围外能力**：35GB/5 亿、MultiPatch 完整拓扑、Raster 和完整 SQL 独立立项。

## 6. 当前证据与计划

- [空间属性联合查询与完整 Feature 流式迭代计划](21_空间属性联合查询实现计划.md)
- [空间属性联合查询代码审核指南](../usage/10_空间属性联合查询代码审核指南.md)
- [FeatureCursor 三轮自检](../evidence/feature-cursor-self-review-2026-07-17.md)
- [联合查询三轮自检](../evidence/spatial-attribute-query-self-review-2026-07-17.md)
- [文档一致性自检](../evidence/spatial-attribute-query-document-audit-2026-07-17.md)
- [功能与基准测试覆盖矩阵](../usage/04_功能与基准测试覆盖矩阵.md)
- [Reader 查询流程](../../src/edgar/explorgdb/reader/QUERY_FLOW.md)

既有最终发布报告和 GDAL 产品矩阵不自动包含本分支新增能力。
