# 注释计划

- **创建日期**：2026-07-18
- **最近更新**：2026-07-18
- **适用分支**：`codex/spatial-attribute-query`
- **当前状态**：四批代码审计与关键注释已完成；完整构建验证受 Actions runner 阻塞

## 1. 目标与边界

本计划用于提升代码库的职责可读性和设计可审计性。注释以中文为主，公开 API 使用
Doxygen；短小、既有英文术语保留。

执行时遵守以下边界：

1. 头文件解释“做什么”：职责、参数、返回值、所有权、生命周期和稳定语义；
2. 实现文件解释“为什么”：索引安全、状态机、回退策略和 fail-closed 原因；
3. 只在复杂算法的关键分支解释“怎么做”，不逐行翻译循环和赋值；
4. 已有充分注释的文件不重复堆叠同义说明；
5. 注释批次不改变函数签名、状态机或算法行为。

计划 22 的 WKB-first 行为修改在注释工作之前独立完成；本文件列出的四批工作只记录
注释和职责审计范围。

## 2. 注释惯例

### 文件头

```cpp
// src/edgar/explorgdb/reader/geometry_model.h
// 几何数据模型 — 定义解码后的拓扑、输出状态和 WKB-first 值对象。
```

### 公开 API

```cpp
/**
 * 从当前 ISO WKB 按需生成 WKT。
 *
 * @return 合法 WKT；损坏或不支持的 WKB 返回 std::nullopt。
 */
std::optional<std::string> to_wkt() const;
```

### 关键实现原因

```cpp
// 候选只暂存在局部 vector；完整叶链验证通过后才发布，确保 fail closed。
```

## 3. 第一批：`common/`

### 审计结果

`binary_reader.*`、`varint.*`、`explorgdb_types.*` 已具备完整的文件头、编码说明和字段注释，
未重复添加低价值注释。

本批补充：

- `utf16.h`：明确 UTF-16LE 代码单元、BMP 限制、Writer 反向转换边界；
- `ole_date.h`：明确 1899-12-30 基准、UTC 解释和格式化参数语义；
- 复核 `FieldDescriptor::wkt` 是空间参考定义文本，不是要素几何输出。

### 验证

使用独立 C++17 编译运行检查验证：

- UTF-16LE BMP 到 UTF-8；
- UTF-8 到 UTF-16 BMP；
- OLE DATE Unix epoch 和日期时间格式化。

结果：通过。

## 4. 第二批：`reader/`

### 公共契约

已补充或重写职责注释：

- `geometry_model.h`：GeometryKind、状态、拓扑模型和 WKB-first `GeometryValue`；
- `gdb_table.h`：record-only、one-pass、Geometry 空占位和扫描器生命周期；
- `query_engine.h`：查询种类、指标、FeatureCursor 租约和引擎生命周期；
- `query_where_internal.h`：WHERE 编译、字段绑定、两种行表示求值和 ATX 可索引边界；
- `curve_geometry.h`：曲线后端、资源上限和顶点索引溢出保护；
- `spatial_predicate.h`：连续网格 bbox 与精确空间复核；
- `wkb_writer.h`：ISO WKB 正式输出合同；
- `wkt_writer.h`：显式调试/兼容输出，不属于默认读取路径。

### 关键实现

已补充：

- `feature_cursor.cpp`：状态机、租约代次、reopen 防护和 WKB-only 指标；
- `query_engine.cpp`：查询分派、活动游标 guard、能力检查和基础回退边界；
- `gdb_attribute_index_query.cpp`：页面偏移、页链循环、条目计数、FID 范围、完整叶链验证和候选延迟发布；
- `wkb_reader.cpp`：严格 WKB 结构验证与按需 WKT；
- `gdb_table_record.cpp`：跳过 Geometry blob 的 record 物化；
- `gdb_table_feature_contract.cpp`：NULL/有效 Geometry 槽统一为空字符串；
- `gdb_table_records.cpp`：批量 records 复用 WKB-first 单条路径。

`curve_geometry.cpp`、`query_engine_combined.cpp` 等长实现保留既有算法结构；其公开职责、
安全边界和关键执行流程已在对应头文件及 `QUERY_FLOW.md` 中说明，未为追求注释数量逐行
污染数值算法。

### 验证

- 纯 C++ WKB→WKT 独立编译运行：通过；
- 大小端、Z/M/ZM、集合、Empty 和损坏输入测试源码已加入；
- record/one-pass Geometry 空占位契约测试已更新；
- 全量 Reader CTest：尚无有效 runner 证据。

## 5. 第三批：`writer/`

`writer_session.h` 与底层表写入器已有较完整架构说明。本批重点补充编辑会话边界：

- `writer_append.h`：完整 sibling staging、副本追加、FID 空洞不复用；
- `writer_update.h`：稳定 FID、显式字段覆盖和单次发布；
- `writer_delete.h`：删除不重排 FID；
- `writer_transaction.h`：多个操作共享一个 working GDB，commit 只替换源一次；
- `writer_recovery.h`：恢复扫描只报告，动作由调用方显式选择，Ambiguous 不自动猜测。

所有修改仅为注释和格式化，公开声明未改变。

## 6. 第四批：`usegdal/`

`datasource.h`、`datasets.h`、`recordset.h`、`feature.h`、`field.h`、`connection_pool.h` 已有
较完整的中文所有权和线程安全说明，审计后保持。

本批重点纠正容易误解的边界：

- `batch_writer.h`：`commit()` 只是 flush + 累计数，不是数据库事务提交；
- `batch_writer.h`：`rollback()` 只丢弃未 flush 缓冲，不能撤销已写入 OGRLayer 的数据；
- `transaction.h`：RAII 只能保证作用域终结，不能补足底层驱动不支持的事务能力。

## 7. 验证状态

### 已获得的有效证据

- `GeometryValue::to_wkt()` 纯 C++ 子集编译与运行通过；
- `common/` 变更的声明与实现独立 C++17 编译运行通过；
- GitHub 文件级静态审计未发现残留 `wkt_write_ms` 源码引用；
- benchmark、one-pass 测试和 package consumer 已同步新 API。

### 尚未获得的证据

截至 2026-07-18，GitHub Actions job 在 runner 分配或 checkout 前失败，返回空 steps/logs；
当前执行环境没有私有仓库 checkout、`gh` 或可用 GitHub 凭据。因此不能声称以下检查通过：

- common、reader、writer、usegdal 每批完整 CMake 构建；
- GDAL OFF Release 完整 CTest；
- GDAL ON Release 完整 CTest；
- 安装后的 package consumer 实际运行；
- schema-v3 100K 性能基准；
- 本地 `git diff --check main...HEAD`。

runner 恢复后应按上述顺序补跑。任何构建失败都应单独修复提交，不得以“仅注释修改”作为
跳过验证的理由。

## 8. 完成判定

本计划的代码审计和关键职责注释已完成。正式验收仍需满足：

1. GDAL OFF/ON Release 均成功构建；
2. 完整 CTest 无非预期失败或 SKIPPED；
3. package consumer 编译、链接、运行；
4. `git diff --check main...HEAD` 无空白错误；
5. 注释批次 diff 不包含非预期行为变化；
6. 计划 22 的 schema-v3 benchmark 生成绑定 SHA 和环境的原始 JSON。
