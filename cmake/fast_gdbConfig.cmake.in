@PACKAGE_INIT@

include(CMakeFindDependencyMacro)
if(@FAST_GDB_CONFIG_WITH_GDAL@)
    find_dependency(GDAL)
endif()

include("${CMAKE_CURRENT_LIST_DIR}/fast_gdbTargets.cmake")
check_required_components(fast_gdb)
